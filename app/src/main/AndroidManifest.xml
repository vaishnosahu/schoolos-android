<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:icon="@drawable/ic_schoolos"
        android:label="SchoolOS"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
        <activity android:name=".features.attendance.AttendanceActivity" android:exported="false"/>
        <activity android:name=".features.messages.MessagesActivity" android:exported="false"/>
        <activity android:name=".features.sfh.StudyFromHomeActivity" android:exported="false"/>
        <activity android:name=".features.results.ResultsActivity" android:exported="false"/>
        <activity android:name=".features.gallery.GalleryActivity" android:exported="false"/>
        <activity android:name=".features.transport.TransportActivity" android:exported="false"/>
        <activity android:name=".features.common.ModuleActivity" android:exported="false"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
